using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BlankRoom : RoomTile
{

    // Start is called before the first frame update
    void Start()
    {
        blank = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
